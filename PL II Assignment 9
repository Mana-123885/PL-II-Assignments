#To study simulations using Simpy
#Assignment 9
import simpy
import random
#1. WAP using SimPy to simulate a single-server queue and analyze customer waiting 
time.
random.seed(10)
a = 3
s = 4
t = 30
x = 0
n = 0
f = 0
w1 = []
while x < t:
    g = random.randint(1, a)
    x = x + g
    if x > t:
        break
    n = n + 1
    d = random.randint(1, s)
    if x >= f:
        b = x
    else:
        b = f
    w = b - x
    w1.append(w)
    f = b + d
    print("Customer", n, "arrived at", x)
    print("Customer", n, "started service at", b)
    print("Customer", n, "waiting time =", w)
    print("Customer", n, "left at", f)
    print()
if len(w1) > 0:
    m1 = sum(w1) / len(w1)
else:
    m1 = 0
print("All waiting times =", w1)
print("Average waiting time =", m1)

#2. Modify the given simulation to use two servers and compare the waiting times with a single-server system.
random.seed(10)

a = 3
s = 4
t = 30
x = 0
n = 0
f1 = 0
f2 = 0
w2 = []
while x < t:
    g = random.randint(1, a)
    x = x + g

    if x > t:
        break
    n = n + 1
    d = random.randint(1, s)
    if f1 <= f2:
        if x >= f1:
            b = x
        else:
            b = f1
        w = b - x
        w2.append(w)
        f1 = b + d
        z = 1
        l = f1
    else:
        if x >= f2:
            b = x
        else:
            b = f2
        w = b - x
        w2.append(w)
        f2 = b + d
        z = 2
        l = f2
    print("Customer", n, "arrived at", x)
    print("Customer", n, "started service at", b, "on server", z)
    print("Customer", n, "waiting time =", w)
    print("Customer", n, "left at", l)
    print()
if len(w2) > 0:
    m2 = sum(w2) / len(w2)
else:
    m2 = 0
print("All waiting times =", w2)
print("Average waiting time =", m2)
print()
print("Comparison:")
print("Single server average =", m1)
print("Two server average =", m2)
if m2 < m1:
    print("Two servers give less waiting time.")
elif m2 > m1:
    print("Single server gives less waiting time.")
else:
    print("Both are same.")

#3. Change the interarrival and service time parameters and study their effect on system performance.
print("Case 1: Fast arrival, slow service")
random.seed(20)
a = 2
s = 5
t = 30
x = 0
n = 0
f = 0
w3 = []
while x < t:
    g = random.randint(1, a)
    x = x + g
    if x > t:
        break
    n = n + 1
    d = random.randint(1, s)
    if x >= f:
        b = x
    else:
        b = f
    w = b - x
    w3.append(w)
    f = b + d
if len(w3) > 0:
    m3 = sum(w3) / len(w3)
else:
    m3 = 0
print("Average waiting time =", m3)
print()
print("Case 2: Slow arrival, fast service")
random.seed(20)
a = 5
s = 2
t = 30
x = 0
n = 0
f = 0
w4 = []
while x < t:
    g = random.randint(1, a)
    x = x + g
    if x > t:
        break
    n = n + 1
    d = random.randint(1, s)
    if x >= f:
        b = x
    else:
        b = f
    w = b - x
    w4.append(w)
    f = b + d
if len(w4) > 0:
    m4 = sum(w4) / len(w4)
else:
    m4 = 0
print("Average waiting time =", m4)
print()
print("Observation:")
print("Case 1 average =", m3)
print("Case 2 average =", m4)
if m3 > m4:
    print("Faster arrival and slower service increase waiting time.")
elif m3 < m4:
    print("Slower arrival and faster service increase waiting time.")
else:
    print("Both cases are same.")

#4. Add code to calculate and display the average waiting time of customers during the simulation.
random.seed(10)
a = 3
s = 4
t = 30
x = 0
n = 0
f = 0
l = []
while x < t:
    g = random.randint(1, a)
    x = x + g
    if x > t:
        break
    n = n + 1
    d = random.randint(1, s)
    if x >= f:
        b = x
    else:
        b = f
    w = b - x
    l.append(w)
    f = b + d
    print("Customer", n, "waiting time =", w)
if len(l) > 0:
    m = sum(l) / len(l)
else:
    m = 0
print("Average waiting time =", m)